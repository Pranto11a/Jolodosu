kind = "web"
previewPath = "/"
title = "জলদস্যু — Pirate Card Game"
version = "1.0.0"
id = "artifacts/card-game"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18956

[services.development]
run = "pnpm --filter @workspace/card-game run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/card-game", "run", "build" ]
publicDir = "artifacts/card-game/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "18956"
BASE_PATH = "/"
